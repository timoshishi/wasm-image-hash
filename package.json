{
  "name": "wasm-img-hash",
  "version": "1.0.0",
  "description": "wasm perceptual image hashing module",
  "engines": "node >= 18.0.0",
  "main": "dist/index.js",
  "types": "typings/index.d.ts",
  "files": [
    "dist",
    "typings",
    "rust/pkg"
  ],
  "scripts": {
    "build:rust": "cd rust && wasm-pack build --target web",
    "build:ts": "tsc",
    "build": "npm run build:rust && npm run build:ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.11",
    "typescript": "^5.0.3"
  }
}
